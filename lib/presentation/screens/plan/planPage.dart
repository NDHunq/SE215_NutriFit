import 'package:flutter/material.dart';

class PlanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Plan Page'),
      ),
      body: Center(
        child: Text('This is the Plan Page'),
      ),
    );
  }
}
